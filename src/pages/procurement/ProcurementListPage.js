import { useEffect,useState } from "react";import {Link} from "react-router-dom";import {getProcurements} from "../../api/procurementApi";import {Page,Loading,Empty,Status} from "../../components/common/Page";import {getApiError} from "../../utils/errors";
export default function ProcurementListPage(){const[data,setData]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");function load(){setLoading(true);getProcurements().then(r=>setData(r.procurements||[])).catch(e=>setError(getApiError(e))).finally(()=>setLoading(false));}useEffect(load,[]);return <Page title="Procurements" subtitle="The authoritative school procurement records." actions={<Link className="btn btn-dark" to="/school/procurements/new">New procurement</Link>}>{error&&<div className="alert alert-danger">{error}</div>}{loading?<Loading/>:data.length===0?<Empty>No procurements found.</Empty>:<div className="card border-0 shadow-sm"><div className="table-responsive"><table className="table align-middle mb-0"><thead><tr><th>Identifier</th><th>Title</th><th>Status</th><th>Required by</th></tr></thead><tbody>{data.map(p=><tr key={p.id}><td><Link to={`/school/procurements/${p.id}`}>{p.procurement_identifier||`#${p.id}`}</Link></td><td>{p.title}</td><td><Status value={p.status}/></td><td>{p.required_by_date||"—"}</td></tr>)}</tbody></table></div></div>}</Page>}
